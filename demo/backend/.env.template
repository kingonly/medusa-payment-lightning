# Your Glow wallet's Lightning address. Get one at https://breez.technology/glow/
LIGHTNING_ADDRESS=you@breez.tips
# Optional: invoice expiry in seconds (default 900 = 15 minutes)
# LIGHTNING_EXPIRY_SECONDS=900

STORE_CORS=http://localhost:8000
ADMIN_CORS=http://localhost:5173,http://localhost:9000
AUTH_CORS=http://localhost:5173,http://localhost:9000,http://localhost:8000
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
# `npm run db` starts an embedded Postgres on this URL; point it at your own instead if you have one.
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:54329/medusa_lightning_demo
